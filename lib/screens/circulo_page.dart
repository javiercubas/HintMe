import 'package:HintMe/model/usuario.dart';
import 'package:flutter/material.dart';

class CirculoPage extends StatelessWidget {
  const CirculoPage({Key? key, required this.usuario, required this.id})
      : super(key: key);
  final Usuario usuario;
  final int id;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
