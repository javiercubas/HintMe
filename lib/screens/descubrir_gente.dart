import 'package:flutter/material.dart';

class DescubrirGente extends StatelessWidget {
  const DescubrirGente({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}